# 影坊工作台 / Flask API（Python 3.10+）
flask>=3.0,<4
flask-cors>=4.0
flask-sqlalchemy>=3.1
sqlalchemy>=2.0
werkzeug>=3.0
requests>=2.31
PyJWT>=2.8,<3
psycopg2-binary>=2.9

# 阿里云 DashScope（qwen-vl-plus、qwen-plus、qwen-image、视频生成等）
dashscope>=1.20

# 二创流水线依赖 video_consistency_agent（YAML 配置、OpenCV 关键帧等）
pyyaml>=6.0
numpy>=1.24
opencv-python-headless>=4.8

# 可选：编排 Agent / 合成链路
moviepy>=1.0.3
pillow>=10.0
aiohttp>=3.9
